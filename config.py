import numpy as np

is_calibration = False
FRAMES_CALIBRATION = 100
num_sensors = 4
count_calibration = 0
array_calibration = []
calibration_numbers_4 = np.array([0, 0, 0, 0]).astype(np.int64)
calibration_numbers_3 = np.array([0, 0, 0]).astype(np.int64)
